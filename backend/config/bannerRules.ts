export const BANNER_RULES = {
  aspectRatio: 3 / 2,
  tolerance: 0.02, // ±2%
  minWidth: 1600,
  minHeight: 900,
  maxFileSizeMB: 5,
  allowedMimeTypes: ["image/jpeg", "image/jpg", "image/webp"],
};
